[project]
name = "anomaly-wiki-shared"
version = "0.1.0"
description = "Shared domain models for ANOMALY-WIKI"
dependencies = [
    "pydantic>=2.0.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]

[dependency-groups]
dev = [
    "pytest>=7.0.0",
]
